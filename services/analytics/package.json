{
  "name": "@services/analytics",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "source .venv/bin/activate && uvicorn app.main:app --reload --port 8001",
    "run": "python app/run.py"
  }
}